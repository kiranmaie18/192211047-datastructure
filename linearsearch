#include<stdio.h>
int main()
{
int a[100],i,n,key;
printf("enter size of array:");
scanf("%d",&n);
printf("enter elements:");
for (i=0;i<n;i++)
{
if (a[i]==key)
{
printf("element found");
}
}
printf("element not found");
}
